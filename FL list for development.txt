1) переместить entityFactory в world (необязательно, можно повременить)
2) убрать необходимость инициализации entityFactory перед использованием. (необязательно)
3) в InputStatePacket для чтения и записи в vector2i используется метод для uint32_t, а не для int. Не критично, но надо исправить
4) выделить отдельный интерфейс для всех классов, что имеют tick
5) Можно также разделить RenderManager и world, а то получается, что мир за рендер отвечает
6) Нужно защитить файлы, такие как json с текстурами от изменения и подмены (Позже)
7) Нужно разделить source файлы по папкам для более удобного поиска
8) можно убрать метод с сериализацией entity в entity
9) Из-за того, что id Entity не переисползуются, возможны коллизии при большом количестве Entity
10) Можно ещё потом на модули с++ перейти, но только тогда, когда код будет почти готов
11) При неподключению к серверу, лог клиента выдаёт какой-то бред
13) Посмотреть по коду и изменить возвращаемое значение сомнительных методов на optional
14) Нужно сделать readme. (пока лень, да и писать особо нечего)
15)нужно как-то убрать сильную зависимость и знания всех использующих bitpacker от конкретных типов. Например, если я захочу изменить размерность size, то мне придётся 
менять запись во всех файлах, проверяющих и использующих size. Предлагаю использовать using для создания типа headerSize. Также нужно под это дело как-то сделать так,
чтобы все использующие headerSize использовали специальный метод для чтения этих данных из пакета, а не метод read_uint32_t
16) переместить методы для работы с векторами из MovementComponent в отдельный файл
17) улучшить упаковку данных для сети, чтобы уменьшить затраты трафика (позже)
18) залогировать FL_SharedLib ++
19) починить метод clean в NetManager
20) написать тесты для различных систем
21) Убрать временное решение в ClientDataLoader и ServerDataLoader
22) сделать так, чтобы проверка getNearestEntityIdsToPosition у CollisionCellMap могла иметь направление
23) в collisionSystem добавить проверку на изменение положения объекта для того, чтобы не проверять коллизию у объектов, которые не двигались
24) залогировать метод getEntityById у worldов
25) залогировать неизвестный CollisionType в stringToCollisionType
27) обработать событие отсоединения клиента от сервера на клиенте
28) Сделать так, чтобы коллизия не спамила бесконечно broadcastом
29) сделать единое хранилище entity в baseWorld, пускай остальные world работают только поверх него и не создают новых
30) нужно добавить в делегаты поимку исключения (если он там нужен, конечно)
31) В контроллере в 94 строчке cpp какие-то магические числа