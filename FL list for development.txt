1) переместить entityFactory в world (необязательно, можно повременить)
2) убрать необходимость инициализации entityFactory перед использованием. (необязательно)
3) как-то сделать так, чтобы TextureManager мог заполнить AnimationsStorage из json
4) клиент не должен знать про json файл с характеристиками entity
5) в InputStatePacket для чтения и записи в vector2i используется метод для uint32_t, а не для int. Не критично, но надо исправить
6) выделить отдельный интерфейс для всех классов, что имеют tick
7) Можно также разделить RenderManager и world, а то получается, что мир за рендер отвечает
8) разделить json на json с анимациями и json с статами
9) Entity не теряет скорость разгона, если стоит на месте
10) нужно у компонентов сериализации добавить проверку на изменение, чтобы не отправлять просто так одинаковые данные
11) Нужно защитить файлы, такие как json с текстурами от изменения и подмены (Позже)
12) Нужно разделить source файлы по папкам для более удобного поиска
13) можно убрать метод с сериализацией entity в entity
14) Из-за того, что id Entity не переисползуются, возможны коллизии при большом количестве Entity
15) Можно ещё потом на модули с++ перейти, но только тогда, когда код будет почти готов
16) При неподключению к серверу, лог клиента выдаёт какой-то бред
17) Нужно, чтобы при авторизации, сервер давал клиенту не только токен, но и EntityData, чтобы он по нему строил Entity.